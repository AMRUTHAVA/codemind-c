#include<stdio.h>
#include<math.h>
int main()
{
    int n,s;
    float S;
    scanf("%d",&n);
    S=sqrt(n);
    s=sqrt(n);
    if(S==s)
    {
        printf("True");
    }
    else
    {
        printf("False");
    }
}